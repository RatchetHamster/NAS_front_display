cd into /home/pi/python (if python doesn't exist, make it)  
git clone this repo  

Make install_servie.sh executable  
chmod u+x /home/pi/python/NAS_front_display/install.sh  

Execute install.sh  
/home/pi/python/NAS_front_display/install.sh  
